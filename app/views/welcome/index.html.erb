<h1><%= t("hello") %></h1>
<div class="container">	
	<div class="row">	
		<% @latest.each do |article| %>
			<div class="col-md-6">
				<h3><%= article.title %></h3>
				<p><%= article.created_at.strftime("%A, %B %d, %Y") %></p>
				<p><%= article.text %></p>
			</div>
		<% end %>	
	</div>
</div>
